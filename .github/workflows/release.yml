name: Release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: hydn-co/build-tools/.github/workflows/connector-release.yml@main
    # Forward org secrets (MESH_MODULES_APP_*) so the reusable workflow can fetch private modules.
    secrets: inherit
